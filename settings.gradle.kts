pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "Sensors_Multiplatform"

include(":androidApp")
include(":shared")